rem  Called via the Project Build Events: Pre-Build Event

rem Other processing can be added as required.

rem TortoiseSVN normally adds itself to the path - if not, then need
rem to find it another way without asking the user where it is :-(

SubWCRev.exe ..\..\.. version.in version.h