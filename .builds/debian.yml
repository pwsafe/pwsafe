image: debian/stable
packages:
  - busybox
sources:
  - https://github.com/Kreyren/pwsafe
tasks:
  - build: |
      cd pwsafe
      sudo apt-get -q update
      sudo apt-get install -qy busybox
      sudo pwsafe/Misc/name-me.sh
      complete-build