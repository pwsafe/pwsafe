#
image: debian
packages:
  - busybox
sources:
  - https://github.com/Kreyren/pwsafe
tasks:
  - build: |
      cd pwsafe
      apt-get -q update
      apt-get install -qy busybox
      Misc/name-me.sh
      complete-build